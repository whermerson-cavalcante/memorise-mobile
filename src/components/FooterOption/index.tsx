import React from 'react'

import { Container } from './styles'

interface Props {
  title: string;
  colms?: number;
}

export function FooterOption({ title, colms = 1 }: Props) {
  return (
    <Container>

    </Container>
  );
}